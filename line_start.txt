                `#include <begin_vertex>\n    vWorldPos = (modelMatrix * vec4(position, 1.0)).xyz;\n    vHeight = vWorldPos.y;\n    float h = max(0.0, vWorldPos.y);\n    // Exposure factor: 0 = sheltered lowland, 1 = exposed hilltop\n    float exposure = smoothstep(0.0, 0.5, h) * smoothstep(3.0, 1.0, h);\n    // Exposed grass: short, stiff, fast twitch (wind-beaten turf)\n    // Sheltered grass: long, lush, slow sway (meadow)\n    float amp = uWindAmp * (1.0 + uHeightBoost * sqrt(min(h, 3.0)));\n    amp *= mix(1.8, 0.4, exposure);  // sheltered=1.8x, exposed=0.4x\n    // Camera distance fade: only sway within ~20m of camera\n    float camDist = length(vWorldPos.xz - uCameraPos.xz);\n    float camFade = smoothstep(25.0, 5.0, camDist);\n    // Mouse exclusion: squares under/around cursor stay rigid\n    float mouseDist = length(vWorldPos.xz - uMousePos.xz);\n    float mouseFade = smoothstep(6.0, 2.0, mouseDist);\n    amp *= camFade * mouseFade;\n    float t = uWindTime;\n    float speedMul = 1.0 + uWindSpeed * 0.25;\n    // Exposed = faster/sharper frequency, sheltered = slower/broader\n    float freqMul = mix(0.5, 2.5, exposure);\n    speedMul *= freqMul;\n    vec2 wind = length(uWindDir) > 0.0 ? normalize(uWindDir) : vec2(1.0, 0.0);\n    // Spatial variance: region-to-region amplitude and frequency differences\n    float regionAmp  = 1.0 + sin(vWorldPos.x * 0.14 + vWorldPos.z * 0.10 + t * 0.15) * 0.5\n                       + sin(vWorldPos.x * 0.06 - vWorldPos.z * 0.08 + t * 