 vWorldPos.z * 0.08 + t * 0.08) * 0.2;\n    float regionFreq = 1.0 + sin(vWorldPos.x * 0.10 - vWorldPos.z * 0.12 + t * 0.12) * 0.5\n                       + sin(vWorldPos.x * 0.04 + vWorldPos.z * 0.06 + t * 0.08) * 0.3;\n    amp *= regionAmp;\n    float phase = dot(vWorldPos.xz, wind) * 2.0 * regionFreq * speedMul;\n    float perpPhase = dot(vWorldPos.xz, vec2(-wind.y, wind.x)) * 2.0 * regionFreq * speedMul;\n    float gust = sin(phase * 2.0 - t * 3.0) * 0.4\n               + sin(phase * 4.5 - t * 2.2) * 0.35\n               + sin(phase * 8.0 - t * 5.5 + perpPhase * 1.5) * 0.25;\n    // Primary downwind displacement\n    transformed.x += (wind.x * 0.9 - wind.y * 0.1) * gust * amp;\n    transformed.z += (wind.y * 0.9 + wind.x * 0.1) * gust * amp;\n    // Coanda swirl on hill flanks: cross-wind sideways shiver (turbulent eddy)\n    float flank = smoothstep(0.3, 1.5, h) * smoothstep(3.0, 1.5, h);  // peak at h=1.0-1.5\n    float swirl = sin(perpPhase * 3.0 - t * 2.7 + vWorldPos.y * 2.0) * 0.3\n                + sin(perpPhase * 6.0 - t * 4.1) * 0.15;\n    transformed.x += (-wind.y * 0.9) * swirl * amp * flank * 0.7;\n    transformed.z += ( wind.x * 0.9) * swirl * amp * flank * 0.7;\n`